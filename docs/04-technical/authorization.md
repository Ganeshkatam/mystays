# Authorization

Authentication does not imply resource access. Enforce server-side capability and resource-level authorization on every sensitive operation. Protect against IDOR/BOLA using ownership, membership, tenancy, provider relationship, or explicit permission checks.