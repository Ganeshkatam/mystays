# Database

Use a relational transactional model for consistency-sensitive domains. Require foreign keys, unique constraints, appropriate indexes, explicit transaction boundaries, migrations, audit timestamps, justified deletion strategies, retention rules, and bounded queries. Search/read models may be separately denormalized.