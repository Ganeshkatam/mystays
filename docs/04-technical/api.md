# API Standards

Define API versioning, resource naming, validation, pagination, filtering, sorting, authentication, authorization, idempotency, rate limiting, correlation IDs, and a deterministic machine-readable error schema. Never trust client authorization or state.