# Security

Report security vulnerabilities privately through the repository's configured security reporting mechanism. Do not publish sensitive vulnerability details in public issues.

Never commit secrets. Validate external input. Enforce server-side authorization. Protect personal data. Keep dependencies and runtime components patched.